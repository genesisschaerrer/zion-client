import Logo from "../images/logo.svg"

export default function LoginNav (){

    return(
        <div className="login-nav">
            <img src={Logo}/>
            <div>finding you a spot so you can hit the trails!</div>
        </div>
    )
}